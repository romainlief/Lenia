# Lenia

## Description

Lenia is a continuous cellular automaton that simulates life-like patterns. This project implements various species and their behaviors.

## Library Installation

To install the required libraries, run the following command:

```bash
pip install -r requirements.txt
```

## Usage

To run the simulation, execute the following command:

```bash
python3 src/main.py
```
